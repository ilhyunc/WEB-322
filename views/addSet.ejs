<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Set - LEGO Collection</title>
    <!-- Add your CSS links here -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css" integrity="sha384-HSMxcRTRxnN+Bdg0JdbxYKrThecOKuH5zCYotlSAcp1+c8xmyTe9GYg1l9a69psu" crossorigin="anonymous">
</head>
<body>
    <!-- Include the navbar partial and pass the 'page' value -->
    <%- include('partials/navbar', { page: '/lego/addSet' }) %>

    <div class="container">
        <br>
        <br>
        <!-- Add a header or title for the page -->
        <h1 class="text-center">Add Set</h1>
        
        <!-- Create your form for adding a new Lego set -->
        <form action="/lego/addSet" method="post">
            <div class="form-group">
                <label for="name">Name:</label>
                <input type="text" id="name" name="name" class="form-control" required style="width: 20em;" >
            </div>

            <div class="form-group">
                <label for="year">Year:</label>
                <input type="number" id="year" name="year" class="form-control" required style="width: 20em;">
            </div>

            <div class="form-group">
                <label for="num_parts">Number of Parts:</label>
                <input type="number" id="num_parts" name="num_parts" class="form-control" required style="width: 20em;">
            </div>

            <div class="form-group">
                <label for="img_url">Image URL:</label>
                <input type="url" id="img_url" name="img_url" class="form-control" required style="width: 20em;">
            </div>

            <div class="form-group">
                <label for="theme_id">Theme:</label>
                <select id="theme_id" name="theme_id" class="form-control" required style="width: 20em;">
                    <!-- Loop through themes to generate options -->
                    <% themes.forEach(theme => { %>
                        <option value="<%= theme.id %>">
                            <%= theme.name %>
                        </option>
                    <% }) %>
                </select>
            </div>

            <div class="form-group">
                <label for="set_num">Set Number:</label>
                <input type="text" id="set_num" name="set_num" class="form-control" required style="width: 20em;">
            </div>

            <!-- Submit button -->
            <button type="submit" class="btn btn-success">Add</button>
        </form>
    </div>

</body>
</html>
